apiVersion: v1
kind: Service
metadata:
  name: seaweedfs-iceberg
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: seaweedfs
    app.kubernetes.io/component: seaweedfs-all-in-one
  ports:
    - name: iceberg
      port: 8181
      targetPort: 8181
      protocol: TCP
